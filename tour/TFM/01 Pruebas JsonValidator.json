{
    "builder": "SIoTRxBuilder",
    "description": "Pruebas del componente JsonValidator:<br> <ol><li>Datos incorrectos en XML.</li><li>Datos correctos en JSON</li><li>Datos incorrectos en texto plano</li></ol>",
    "user_template": "<#-- Se asignan los datos en una lista: el primer elemento son datos en XML, después en formato JSON y finalmente, en texto plano -->\r<#assign inputData=[\"<product><name>Test</name></product>\", \"{ \"product\": { \"name\": \"Test\"} }\", \"test\"]>\r\r<#-- Se itera sobre la lista anterior -->\r<#list inputData as input>\r\r\t<#-- Se llama al componente JsonValidator de tipo action para validar que cada dato de la lista anterior esté en formato JSON -->\r    <@action type=\"JsonValidator\" data=input; dataR>\r        <#-- Transforma los resultados obtenidos a JSON para tratarlos -->\r        <#assign result=dataR?eval>\r\r        <#if result.status == \"ok\">\r\t     <#-- Si el estado es “ok”, se mostrará este mensaje -->\r            Data [=input] is a valid JSON schema.\r        <#else>\r\t     <#-- Si el estado es erróneo, se mostrará este mensaje -->\r            Data [=input] is NOT a JSON schema.\r        </#if>\r    </@>\r</#list>\r",
    "solution_template" : "<#-- Se asignan los datos en una lista: el primer elemento son datos en XML, después en formato JSON y finalmente, en texto plano -->\r<#assign inputData=[\"<product><name>Test</name></product>\", \"{ \"product\": { \"name\": \"Test\"} }\", \"test\"]>\r\r<#-- Se itera sobre la lista anterior -->\r<#list inputData as input>\r\r\t<#-- Se llama al componente JsonValidator de tipo action para validar que cada dato de la lista anterior esté en formato JSON -->\r    <@action type=\"JsonValidator\" data=input; dataR>\r        <#-- Transforma los resultados obtenidos a JSON para tratarlos -->\r        <#assign result=dataR?eval>\r\r        <#if result.status == \"ok\">\r\t     <#-- Si el estado es “ok”, se mostrará este mensaje -->\r            Data [=input] is a valid JSON schema.\r        <#else>\r\t     <#-- Si el estado es erróneo, se mostrará este mensaje -->\r            Data [=input] is NOT a JSON schema.\r        </#if>\r    </@>\r</#list>\r",
    "expected_result": "Data <product><name>Test</name></product> is NOT a JSON schema.\r\r\r            Data { \"product\": { \"name\": \"Test\"} } is a valid JSON schema.\r\r\r            Data test is a valid JSON schema."
}
